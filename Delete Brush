javascript:(function(){var style=document.createElement('style');style.innerHTML='*:hover{outline:2px solid red!important;}';document.head.appendChild(style);var del=e=>{e.preventDefault();e.target.remove();};document.addEventListener('click',del,true);document.addEventListener('keydown',e=>{if(e.key==='Escape'){document.removeEventListener('click',del,true);style.remove();alert('Eraser deactivated.');}},{once:true});})();
